#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
	int n;
{
	if (n < 0)
	{
		printf("%d is negative\n",n);
	} else if (n > 0)
	{
		print("%d is positive\n",n);
	} else 
	{
		print("%d is zero\n",n);
	}
	return (0);
}
